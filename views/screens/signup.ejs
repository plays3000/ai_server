<!--
사용자데이터베이스테이블컬럼제안

== 사용자 (주요 및 필수정보) ==
- 고유아이디: UUID, Primary Key, NOT NULL, 변경불가
- 사용자아이디: String/VarChar, NOT NULL, 변경가능, 다언어문자지원
- 사용자암호: String/VarChar, NOT NULL, 변경가능, 다언어문자지원, 암호화적용
- 가입날짜: DateTime, NOT NULL, 변경불가

== 로그인내력 ==
- 오르인사용자고유아이디: UUID, 사용자.고유아이디, NOT NULL, 변경불가
- 로그인날짜: DateTime. NOT NULL, 변경불가

== 사용자상세정보 --
- 고유아이디: UUID, Primary Key, 사용자.고유아이디, NOT NULL, 변경불가
- 사용자이름: String/VarChar, 변경가능
- 설명란: String/VarChar, 변경가능
- 상세소개: String/VarChar. 변경가능
- 생년월일: Date, 변경가능
- 전자우편: String/VarChar, 변경가능
- 휴대전화: String/VarChar, 변경가능
- 성별: Byte/Number, 변경가능, 미등록, 남, 녀

== 직원상세정보 ==
- 고유아이디: UUID, Primary Key, 사용자.고유아이디, NOT NULL, 변경불가
- 사용자이름: String/VarChar, 사용자상세정보.사용자이름, 변경가능
- 직급: String/VarChar, 변경가능
- 소속 및 부서: String/VarChar, 변경가능

== 그룹 ==
- 그룹식별자: UUID, Primary Key, NOT NULL, 변경불가
- 그룹명: String/VarChar, NOT NULL, 변경가능
- 사업자번호: String/VarChar, 변경가능

== 법인 ==
- 법인식별자: UUID, Primary Key, NOT NULL, 변경불가
- 회사명: String/VarChar, NOT NULL, 변경가능
- 소속그룹코드: String/VarChar, 변경가능
- 대표자직급: String/VarChar, 변경가능
- 법인생성닐짜: Date, NOT NULL, 변경가능
-->

<div class="login-container signup-container">
    <h2>회원가입</h2>
    <form id="signupForm">
        <div class="section-title">기본 정보</div>
        
        <div class="form-row">
            <div class="form-group required-field">
                <label for="id">아이디 <span class="req">*</span></label>
                <input type="text" id="id" class="form-input" placeholder="아이디 입력" required>
            </div>
            <div class="form-group required-field">
                <label for="id-confirm">아이디 확인 <span class="req">*</span></label>
                <input type="text" id="id-confirm" class="form-input" placeholder="아이디 재입력" required>
            </div>
        </div>

        <div class="form-row">
            <div class="form-group required-field">
                <label for="password">비밀번호 <span class="req">*</span></label>
                <input type="password" id="password" class="form-input" placeholder="비밀번호 입력" required>
            </div>
            <div class="form-group required-field">
                <label for="password-confirm">비밀번호 확인 <span class="req">*</span></label>
                <input type="password" id="password-confirm" class="form-input" placeholder="비밀번호 재입력" required>
            </div>
        </div>

        <div class="form-row">
            <div class="form-group required-field">
                <label for="personal-name">이름 <span class="req">*</span></label>
                <input type="text" id="personal-name" class="form-input" placeholder="실명 입력" required>
            </div>
            <div class="form-group required-field">
                <label for="phone">전화번호 <span class="req">*</span></label>
                <input type="tel" id="phone" class="form-input" placeholder="010-0000-0000" required>
            </div>
        </div>
        
        <div class="form-group required-field">
            <label for="email">이메일 <span class="req">*</span></label>
            <input type="email" id="email" class="form-input" placeholder="example@email.com" required>
        </div>

        <div class="divider"><span>가입 유형 선택</span></div>
        
        <div class="role-selector">
            <label class="role-card active" onclick="selectRole('group')">
                <input type="radio" name="role" value="group" checked hidden>
                <div class="icon"><i class="fas fa-network-wired"></i></div>
                <div class="title">그룹 창설</div>
                <div class="desc">여러 회사를 관리하는<br>그룹 관리자</div>
            </label>

            <label class="role-card" onclick="selectRole('company')">
                <input type="radio" name="role" value="company" hidden>
                <div class="icon"><i class="fas fa-building"></i></div>
                <div class="title">법인 창설</div>
                <div class="desc">독립된 회사 또는<br>그룹 산하 법인</div>
            </label>

            <label class="role-card" onclick="selectRole('employee')">
                <input type="radio" name="role" value="employee" hidden>
                <div class="icon"><i class="fas fa-user-tie"></i></div>
                <div class="title">일반 직원</div>
                <div class="desc">초대 코드를 통해<br>회사에 소속</div>
            </label>
        </div>

        <div class="role-fields-container">
            
            <div id="fields-group" class="role-section">
                <div class="form-group required-field">
                    <label>그룹명 <span class="req">*</span></label>
                    <input type="text" name="group_name" class="form-input" placeholder="설립할 그룹명을 입력하세요">
                </div>
                <div class="form-group required-field">
                    <label>사업자등록번호 <span class="req">*</span></label>
                    <input type="text" name="business_num" class="form-input" placeholder="- 없이 숫자만 입력">
                </div>
            </div>

            <div id="fields-company" class="role-section" style="display: none;">
                <div class="form-group required-field">
                    <label>회사명 (법인명) <span class="req">*</span></label>
                    <input type="text" name="company_name" class="form-input" placeholder="회사 이름을 입력하세요">
                </div>
                <div class="form-row">
                    <div class="form-group optional-field">
                        <label>소속 그룹 코드 (선택)</label>
                        <input type="text" name="parent_group_code" class="form-input" placeholder="그룹 코드가 있다면 입력">
                    </div>
                    <div class="form-group optional-field">
                        <label>대표자 직급 (선택)</label>
                        <input type="text" name="rep_position" class="form-input" placeholder="예: 대표이사, CEO">
                    </div>
                </div>
            </div>

            <div id="fields-employee" class="role-section" style="display: none;">
                <div class="form-group required-field">
                    <label>회사 초대 코드 <span class="req">*</span></label>
                    <input type="text" name="invite_code" class="form-input" placeholder="전달받은 초대 코드를 입력하세요">
                </div>
                <div class="form-row">
                    <div class="form-group optional-field">
                        <label>부서명 (선택)</label>
                        <input type="text" name="department" class="form-input" placeholder="소속 부서">
                    </div>
                    <div class="form-group optional-field">
                        <label>직급 (선택)</label>
                        <input type="text" name="position" class="form-input" placeholder="본인 직급">
                    </div>
                </div>
            </div>
        </div>

        <div class="btn-group mt-20">
            <button type="submit" class="btn btn-primary btn-login">회원가입 완료</button>
            <button type="button" class="btn btn-signup" onclick="history.back()">뒤로가기</button>
            <button type="reset" class="btn btn-reset">초기화</button>
        </div>
    </form>
</div>
<script>
    function selectRole(role) {
        // 1. 모든 카드 스타일 초기화
        document.querySelectorAll('.role-card').forEach(card => card.classList.remove('active'));
        
        // 2. 선택된 카드 활성화
        const selectedInput = document.querySelector(`input[value="${role}"]`);
        if(selectedInput) {
            selectedInput.parentElement.classList.add('active');
        }

        // 3. 모든 입력 섹션 숨김
        document.querySelectorAll('.role-section').forEach(sec => sec.style.display = 'none');

        // 4. 선택된 섹션 보이기
        document.getElementById(`fields-${role}`).style.display = 'block';
    }
</script>