<div id="settingModal" class="modal-overlay">
    <div class="modal-window">
        <div class="modal-header">
            <h2 data-i18n="modal_title">환경설정</h2>
            <span class="close-btn" id="closeModalX">&times;</span>
        </div>
        <div class="modal-body">
            <div class="setting-row">
                <label data-i18n="label_theme">화면테마</label>
                <select id="themeSelect">
                    <option value="light" data-i18n="">밝은 모드</option>
                    <option value="dark" data-i18n="">어두운 모드</option>
                </select>
            </div>
            <div class="setting-row">
                <label data-i18n="label_language">언어설정</label>
                <select id="languageSelect"></select>
            </div>
            <div class="setting-row">
                <label data-i18n="label_writing_mode">쓰기방향</label>
                <select id="writingModeSelect">
                    <option value="horizontal-ltr" data-i18n="opt_horizontal_ltr">가로쓰기 (좌 → 우: 기본)</option>
                    <option value="horizontal-rtl" data-i18n="opt_horizontal_rtl">가로쓰기 (우 → 좌: 아람/히브리)</option>
                    <option value="vertical-rl" data-i18n="opt_vertical_rl">세로쓰기 (우 → 좌: 한/중/일)</option>
                    <option value="vertical-lr-mongolian" data-i18n="opt_vertical_lr_mongolian">세로쓰기 (좌 → 우: 몽골/만주)</option>
                    <option value="vertical-lr-maya" data-i18n="opt_vertical_lr_maya">세로쓰기 (좌 → 우: 마야/블록)</option>
                </select>
            </div>
        </div>
        <div class="modal-footer">
            <button id="btnApply" class="btn-secondary" data-i18n="btn_apply">적용</button>
            <button id="btnOk" class="btn-primary" data-i18n="btn_confirm">확인</button>
        </div>
    </div>
</div>